# Make vim the default editor
export EDITOR="vim"

# Larger bash history (allow 32³ entries; default is 500)
export HISTSIZE=32768
export HISTFILESIZE=$HISTSIZE
export HISTCONTROL=ignoredups
# Make some commands not show up in history
export HISTIGNORE="ls:cd:cd -:pwd:exit:date:* --help"

# Prefer US English and use UTF-8
export LANG="en_US"
export LC_ALL="en_US.UTF-8"

# Highlight section titles in manual pages
export LESS_TERMCAP_md="$ORANGE"

# Don’t clear the screen after quitting a manual page
export MANPAGER="less -X"

# current endcards directory
export cards="/Users/joshuaconner/html5-endcards/$(date "+%Y/%m")/"

shopt -s cdable_vars

# enable JRebel for scalatra dev
export SCALATRA_JREBEL="/Users/joshuaconner/jrebel/jrebel.jar"

export NODE_PATH="/usr/local/lib/node_modules:/usr/local/share/npm/lib/node_modules"

export RBENV_ROOT=/usr/local/var/rbenv

export EC2_HOME=/Users/joshuaconner/ec2-api-tools

export GOPATH="$(brew --prefix)/Cellar/go/1.2.1/libexec/bin"
export PATH="$PATH:$GOPATH/bin"

# no ansible cows
export ANSIBLE_NOCOWS=1

export DOCKER_HOST=tcp://localhost:4243

export JAVA_HOME=$(/usr/libexec/java_home)
export SCALA_HOME=$(brew --prefix)/Cellar/scala/2.9.2/libexec
export JAVACMD=$(brew --prefix)/bin/drip
export DRIP_SHUTDOWN=30
export SBT_OPTS="-XX:+UseConcMarkSweepGC -XX:+CMSClassUnloadingEnabled -XX:PermSize=128M -XX:MaxPermSize=512M"

# export NEBULA_ENV="josh"
